const App = require('../components/App');
// const ReactTestRenderer = require('react-test-renderer');

describe('<App />', () => {
  it('should render a div with id site-container', () => {
  });
});
